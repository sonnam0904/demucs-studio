{
  "name": "demucs-studio-release",
  "private": true,
  "description": "Release tooling only. The app is Go + Wails; the UI has its own package.json in frontend/.",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.3"
  }
}
